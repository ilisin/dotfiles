tasks:
    default:
        links:
            - [".bashrc"]
            - [".bash_aliases"]
            - [".gitconfig"]
            - [".gitignore"]
            - [".profile"]
    Mac:
        links:
            - [".gitconfig", "mac/.gitconfig"]
            - [".gitignore"]
    unknow:
        cmds:
            - ["echo", "can't install dotfile in unknow OS."]
